"""A GUI for the parts of deck.json that are about how the deck looks.

Themes and settings only. Pages, buttons and actions are read — the preview would be useless
without them — but never written, which is what keeps this a bounded tool rather than a second
implementation of the whole format.
"""

__version__ = "0.1.0"
